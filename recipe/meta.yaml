{% set name = "kanapy" %}
{% set version = "6.5.1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/ICAMS/Kanapy/archive/refs/tags/v{{ version }}.tar.gz
  sha256: d8ed8a9b296444cd433ab07cd9a30b0c5c4804cc9fba42276d1f5e26113963bf

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  skip: true  # [win]
  skip: true  # [py>=312]

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ stdlib("c") }}
    - cmake
    - make
  host:
    - pip
    - python
    - setuptools
  run:
    - python
    - numpy
    - matplotlib-base
    - scipy
    - seaborn
    - click
    - tqdm
    - pytest-cov
    - pytest-mock
    - pytest
    - jsonschema
    - networkx
    - orix
    - scikit-image
    - requests

test:
  imports:
    - kanapy
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/mrgprasad/kanapy
  license: AGPL-3.0-or-later
  license_family: GPL
  license_file: LICENSE
  summary: 'A python package for generating complex synthetic polycrystalline microstructures.'
  description: |
    Kanapy is a python package for generating complex synthetic 
    polycrystalline microstructures. The general implementation is done 
    in Python with the performance critical part for the geometry module 
    implemented in C++. 
  doc_url: https://github.com/mrgprasad/kanapy
  dev_url: https://github.com/mrgprasad/kanapy

extra:
  recipe-maintainers:
    - jan-janssen
